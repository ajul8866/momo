# Source Evidence For the vuln-mine Real Run

This is lookup material. Read [one-real-run.md](../walkthroughs/one-real-run.md) first if the behavior is not clear yet.

Evidence was collected over commit `563dc81366bf694917dc7dfbf0a9475f01012a0f`.

Working behavior: `/vuln-mine example` mining the built-in `naiveparse` ASan target — the same path whose finished state is checked in as `runs/smoke-example/`.

## Evidence Traversal Log

| Pass | Purpose | Inspected evidence | What changed in the model |
| --- | --- | --- | --- |
| Pass 1 | Map the main path. | [`SKILL.md`](../../.claude/skills/vuln-mine/SKILL.md) (both skills), [`explore-loop.js`](../../.claude/skills/vuln-mine/workflow/explore-loop.js), [`init-memory.sh`](../../.claude/skills/vuln-mine/helpers/init-memory.sh), [`manifest.yaml`](../../.claude/skills/vuln-mine/manifests/example/manifest.yaml), [`runs/smoke-example/*.yaml`](../../runs/smoke-example/). | Established how one `/vuln-mine` call becomes a verified PoC: INIT seeds 7 memory files, EXPLORE runs Reader→Synthesizer→Analyst batches, REPORT reads them back. |
| Pass 2 | Socratic challenge and fill the model. | [`classify-result.sh`](../../.claude/skills/vuln-mine/helpers/classify-result.sh), [`run-harness.sh`](../../.claude/skills/vuln-mine/helpers/run-harness.sh), [`validate-manifest.sh`](../../.claude/skills/vuln-mine/helpers/validate-manifest.sh), [`write-back.sh`](../../.claude/skills/vuln-mine/helpers/write-back.sh), [`recompute-stagnation.sh`](../../.claude/skills/vuln-mine/helpers/recompute-stagnation.sh), [`verify-crash.sh`](../../.claude/skills/vuln-target-prep/helpers/verify-crash.sh), both `run-all.sh` suites, validate fixtures, the `naiveparse.c` bug site. | Added the hard parts: deterministic crash classification, the ASan `abort_on_error` carry-forward, the `harness_cmd` injection gate, flock+atomic write under parallel agents, stagnation persistence, and the prep→mine contract gate. |

Coverage note: this map covers the mining loop and the trust-boundary gates that protect it. It does not trace the `vuln-target-prep` GitHub-clone path in depth — that live-network path is fixture-tested (`test-smoke-prep.sh`) but a real clone is the documented first end-to-end test ([README limitations](../../README.md)). The example target is taken as already-prepped, which is exactly how the smoke run and the validator exercise it.

| Claim | Evidence | Confidence | Caveat | Used by |
| --- | --- | --- | --- | --- |
| `/vuln-mine <name>` resolves a manifest under `vuln-mine/manifests/<NAME>/manifest.yaml` and aborts if absent. | [`SKILL.md`](../../.claude/skills/vuln-mine/SKILL.md) `## 1. INIT` step 1 | Confirmed | Manifest is operator-authored; that is why a validator gate exists. | README, walkthrough |
| INIT seeds exactly 7 YAML files plus a non-budgeted baseline run in `06-verification.yaml`. | [`init-memory.sh`](../../.claude/skills/vuln-mine/helpers/init-memory.sh) step 5 (the `files` dict) and step 6 | Confirmed | The baseline run is recorded but does not consume a budget iteration. | walkthrough, structured-memory |
| The explore loop is Reader→Synthesizer→Analyst, run in batches of 3, driven by the Workflow tool. | [`explore-loop.js`](../../.claude/skills/vuln-mine/workflow/explore-loop.js) `body`, `BATCH = 3`, `pipeline(...)` | Confirmed | Workflow agents cannot be invoked from a subagent; SKILL.md §4 documents a reduced manual simulation fallback. | walkthrough, structured-memory |
| A crash does NOT stop the loop — it keeps mining to collect multiple deduped PoCs until budget hits a 10% REPORT floor. | [`explore-loop.js`](../../.claude/skills/vuln-mine/workflow/explore-loop.js) `FLOOR = 10%`, `while (total - used > FLOOR)`; SKILL.md §2 | Confirmed | Floor only applies when `budget_total > 0`. | walkthrough, README |
| Crash verdict is deterministic (exit code + sanitizer regex), not LLM judgment. | [`classify-result.sh`](../../.claude/skills/vuln-mine/helpers/classify-result.sh) §7.1 table and `signal_from_exit` | Confirmed | LLM only writes the result back; it never decides crash/benign. | deterministic-classification |
| Stagnation counter ≥ 3 forces the next Synthesizer to switch vector. | [`explore-loop.js`](../../.claude/skills/vuln-mine/workflow/explore-loop.js) `switchVector = stagnation >= 3`; [`recompute-stagnation.sh`](../../.claude/skills/vuln-mine/helpers/recompute-stagnation.sh) | Confirmed | Counter is read back from disk to avoid a double-bump each batch. | structured-memory |
| ASan exits 1 by default; the harness forces `ASAN_OPTIONS=abort_on_error=1` for `asan` targets so a crash surfaces as SIGABRT/134 and matches `acceptable_signals`. | [`run-harness.sh`](../../.claude/skills/vuln-mine/helpers/run-harness.sh) lines 39–41; SKILL.md §2 "ASan carry-forward" | Confirmed | Only set when the caller has not already exported `ASAN_OPTIONS`. | walkthrough, deterministic-classification |
| `validate-manifest.sh` is the trust-boundary gate: it rejects shell metacharacters in `harness_cmd` outside the two allowed placeholders. | [`validate-manifest.sh`](../../.claude/skills/vuln-mine/helpers/validate-manifest.sh) step 6; fixture [`injection.yaml`](../../.claude/skills/vuln-mine/helpers/tests/fixtures/validate/injection.yaml) (`a; rm -rf /`) | Confirmed | Only `{{binary}}` and `{{input}}` may carry a shell-meaningful value. | walkthrough, manifest-contract |
| `write-back.sh` serializes writers with `flock` and replaces atomically; `verified_crashes` and `mined_areas` dedup. | [`write-back.sh`](../../.claude/skills/vuln-mine/helpers/write-back.sh) | Confirmed | Verified by `test-write-back.sh` under concurrent writes. | structured-memory |
| `harness_cmd` substitutes only `{{binary}}` and `{{input}}` before running under `timeout`. | [`run-harness.sh`](../../.claude/skills/vuln-mine/helpers/run-harness.sh) lines 44–45 | Confirmed | Manifest is pre-validated, so only those two placeholders are trusted. | manifest-contract |
| The smoke run on the example target reaches a verified crash at `parse_np naiveparse.c:22` (SIGABRT/ASan, exit 134). | [`04-candidate-poc.yaml`](../../runs/smoke-example/04-candidate-poc.yaml), [`06-verification.yaml`](../../runs/smoke-example/06-verification.yaml) | Confirmed | `runs/` is gitignored; `smoke-example/` is the checked-in proof and must be regenerated by re-running INIT+EXPLORE. | walkthrough, README |
| The example target's bug is an unbounded `memcpy` of an attacker-controlled length into a 64-byte stack buffer. | [`naiveparse.c`](../../.claude/skills/vuln-mine/manifests/example/src/naiveparse.c) line 22 | Confirmed | Deliberate SUT bug, not production code. | walkthrough, commands |
| A PostToolUse hook auto-runs the vuln-mine helper test suite on edits under `helpers/*.sh`. | [`.claude/settings.json`](../../.claude/settings.json) `hooks.PostToolUse` | Confirmed | Hook is itself gitignored; behavior is real on the dev host. | README, commands |

Falsifying checks: if `validate-manifest.sh` did not reject `a; rm -rf /`, `test-validate-manifest.sh` would fail ("injection harness_cmd fails"). If write-back lost records under concurrency, `test-write-back.sh` would fail ("exactly 4 candidates"). If the ASan carry-forward were wrong, the smoke run's `verified_crashes` would be empty and `04-candidate-poc.yaml` would show no SIGABRT. Both suites were green at collection time (see change-log).

Likely reader follow-up: "how does a target get from a GitHub URL to the manifest this loop consumes?" — answered in [the manifest-contract reference](manifest-contract.md) and summarized in [structured-memory](../modules/structured-memory.md); the live-network prep path is labeled as fixture-tested only.

Evidence status: Confirmed unless a row says otherwise.
